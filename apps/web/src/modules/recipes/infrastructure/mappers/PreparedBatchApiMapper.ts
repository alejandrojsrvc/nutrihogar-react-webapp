import type { PreparedBatch, PreparedBatchDetails, PreparedFoodLeftover, ServedPortion } from '../../domain/PreparedBatch';
const record = (value: unknown): Record<string, unknown> => value && typeof value === 'object' ? value as Record<string, unknown> : {};
const numbers = (value: unknown) => Object.fromEntries(Object.entries(record(value)).map(([key, amount]) => [key, Number(amount)]));
export function toPreparedBatch(value: unknown): PreparedBatch { const source = record(value); return { cancelledAt: source.cancelledAt == null ? null : String(source.cancelledAt), createdAt: String(source.createdAt ?? ''), finalCookedWeight: source.finalCookedWeight == null ? null : Number(source.finalCookedWeight), finalizedAt: source.finalizedAt == null ? null : String(source.finalizedAt), householdId: String(source.householdId ?? ''), id: String(source.id ?? ''), ingredients: Array.isArray(source.ingredients) ? source.ingredients.map((raw) => { const item = record(raw); return { baseQuantity: item.baseQuantity == null ? null : Number(item.baseQuantity), baseUnit: item.baseUnit == null ? null : String(item.baseUnit), foodId: String(item.foodId ?? ''), foodNameSnapshot: item.foodNameSnapshot == null ? null : String(item.foodNameSnapshot), id: String(item.id ?? ''), notes: item.notes == null ? null : String(item.notes), nutrients: numbers(item.nutrients), position: Number(item.position ?? 0), preparationStateSnapshot: item.preparationStateSnapshot == null ? null : String(item.preparationStateSnapshot), quantity: Number(item.quantity ?? 0), servingId: item.servingId == null ? null : String(item.servingId), unit: String(item.unit ?? '') }; }) : [], nutrientsPer100Grams: numbers(source.nutrientsPer100Grams), nutrientsPerGram: numbers(source.nutrientsPerGram), preparedAt: String(source.preparedAt ?? ''), recipeId: source.recipeId == null ? null : String(source.recipeId), recipeNameSnapshot: String(source.recipeNameSnapshot ?? ''), status: String(source.status ?? 'DRAFT') as PreparedBatch['status'], totalNutrients: numbers(source.totalNutrients), updatedAt: String(source.updatedAt ?? ''), warnings: Array.isArray(source.warnings) ? source.warnings as PreparedBatch['warnings'] : [] }; }
export function toPreparedBatchDetails(value: unknown): PreparedBatchDetails { const source = record(value); return { availability: source.availability == null ? null : (() => { const item = record(source.availability); return { availableWeight: Number(item.availableWeight ?? 0), discardedWeight: Number(item.discardedWeight ?? 0), finalCookedWeight: Number(item.finalCookedWeight ?? 0), savedRemainderWeight: Number(item.savedRemainderWeight ?? 0), servedWeight: Number(item.servedWeight ?? 0), storedLeftoverWeight: Number(item.storedLeftoverWeight ?? 0) }; })(), batch: toPreparedBatch(source.batch), leftovers: Array.isArray(source.leftovers) ? source.leftovers.map(toLeftover) : [], servedPortions: Array.isArray(source.servedPortions) ? source.servedPortions.map(toPortion) : [] }; }
function toPortion(value: unknown): ServedPortion { const item = record(value); const remainder = record(item.remainder); return { adultProfileId: String(item.adultProfileId ?? ''), consumedWeight: item.consumedWeight == null ? null : Number(item.consumedWeight), id: String(item.id ?? ''), mealId: item.mealId == null ? null : String(item.mealId), nutritionSnapshot: numbers(item.nutritionSnapshot), remainder: item.remainder == null ? null : { createdAt: String(remainder.createdAt ?? ''), disposition: String(remainder.disposition ?? ''), weight: Number(remainder.weight ?? 0) }, servedAt: String(item.servedAt ?? ''), servedWeight: Number(item.servedWeight ?? 0), status: String(item.status ?? '') }; }
export function toLeftover(value: unknown): PreparedFoodLeftover { const item = record(value); return { availableWeight: Number(item.availableWeight ?? 0), createdAt: String(item.createdAt ?? ''), id: String(item.id ?? ''), nutrientDensitySnapshot: numbers(item.nutrientDensitySnapshot), notes: item.notes == null ? null : String(item.notes), preparedBatchId: String(item.preparedBatchId ?? ''), status: String(item.status ?? ''), storageLocation: item.storageLocation == null ? null : String(item.storageLocation), storedAt: String(item.storedAt ?? ''), updatedAt: String(item.updatedAt ?? '') }; }
