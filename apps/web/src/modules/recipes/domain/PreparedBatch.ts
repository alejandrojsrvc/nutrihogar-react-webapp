export type PreparedBatchStatus = 'DRAFT' | 'INGREDIENTS_CONFIRMED' | 'FINALIZED' | 'CANCELLED';
export interface PreparedBatchIngredient { id: string; foodId: string; servingId: string | null; quantity: number; unit: string; position: number; notes: string | null; foodNameSnapshot: string | null; preparationStateSnapshot: string | null; baseQuantity: number | null; baseUnit: string | null; nutrients: Record<string, number>; }
export interface PreparedBatch { id: string; householdId: string; recipeId: string | null; recipeNameSnapshot: string; preparedAt: string; status: PreparedBatchStatus; ingredients: PreparedBatchIngredient[]; totalNutrients: Record<string, number>; finalCookedWeight: number | null; nutrientsPerGram: Record<string, number>; nutrientsPer100Grams: Record<string, number>; warnings: Array<{ ingredientId: string; foodId: string; code: string; message: string }>; createdAt: string; updatedAt: string; finalizedAt: string | null; cancelledAt: string | null; }
export interface PreparedBatchAvailability { finalCookedWeight: number; servedWeight: number; storedLeftoverWeight: number; savedRemainderWeight: number; discardedWeight: number; availableWeight: number; }
export interface ServedPortion { id: string; adultProfileId: string; servedWeight: number; servedAt: string; status: string; consumedWeight: number | null; remainder: { weight: number; disposition: string; createdAt: string } | null; mealId: string | null; nutritionSnapshot: Record<string, number>; }
export interface PreparedFoodLeftover { id: string; preparedBatchId: string; availableWeight: number; nutrientDensitySnapshot: Record<string, number>; storedAt: string; storageLocation: string | null; notes: string | null; status: string; createdAt: string; updatedAt: string; }
export interface PreparedBatchDetails { batch: PreparedBatch; availability: PreparedBatchAvailability | null; servedPortions: ServedPortion[]; leftovers: PreparedFoodLeftover[]; }
